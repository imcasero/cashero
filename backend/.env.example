# Entorno de ejecucion: local | staging | production
ENVIRONMENT=local

# Origenes permitidos por CORS, separados por comas.
# El frontend de Vite corre en el 5173 (ver ../dev.sh).
CORS_ORIGINS=http://localhost:5173
